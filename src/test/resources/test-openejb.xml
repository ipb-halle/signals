<?xml version="1.0"?>
<openejb>
    <Resource id="testDS" type="javax.sql.DataSource">
        jdbcDriver = org.postgresql.Driver
        jdbcUrl = jdbc:postgresql://localhost:65432/signals?charSet=UTF-8
        jtaManaged = true
        userName = signals
        password = signals
    </Resource>
    <Resource id="signalsConfig" class-name="de.ipb_halle.signals.SignalsConfig">
      apiKey = Honestly+we+would+not+like+to+disclose+the+API+Key+in+our+source+code==
      baseUrl = https://endpoint.somewhere.invalid/api/rest/v1.0
      groupAttrDescription = LDAP managed group
      ldapAttrAccountExpirationDate = accountExpires
      ldapAttrAlias = initials
      ldapAttrCreatedAt = whenCreated
      ldapAttrEmail = userPrincipalName
      ldapAttrFirstName = givenName
      ldapAttrGroupName = cn
      ldapAttrLastName = sn
      ldapAttrMemberOf = memberOf
      ldapAttrMembers = member
      ldapAttrObjectClass = objectClass
      ldapAttrObjectClassGroup = group
      ldapAttrObjectClassUser = person
      ldapAttrUserName = userPrincipalName
      ldapContextProviderURL = ldap://ldap.somewhere.invalid:389
      ldapContextReferral = follow
      ldapDateFormatString = yyyyMMddHHmmss.S'Z'
      ldapDeniedUsers = CN=testDeniedUsers, OU=groups, DC=somewhere, DC=invalid
      ldapManagedGroups = CN=testGroups, OU=groups, DC=somewhere, DC=invalid
      ldapManagedRoles = CN=testRoles, OU=groups, DC=somewhere, DC=invalid
      ldapManagedUsers = CN=testUsers, OU=groups, DC=somewhere, DC=invalid
      ldapSecurityPrincipal = CN=ldapsearch, CN=admin, DC=somewhere, DC=invalid
      ldapSecurityCredentials = secretPassword
      ldapSecurityAuthentication = simple
      mailFrom = someone@somewhere.invalid
      mailTo = someone@somewhere.invalid
      snbInstanceName = Unit Test
      standardUserRoleName = Standard User
      userAttrCountry = Germany
      userAttrLicenses = dummyLicense
      userAttrOrganization = testOrg
    </Resource>
</openejb>

